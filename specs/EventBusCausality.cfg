\* Clean model: envelope gating enforces causal ordering.
\* Expected: no error, no invariant violation.
SPECIFICATION Spec
CONSTANTS
  A = {a1, a2}
  R = {r1, r2}
CONSTRAINT LogBound
INVARIANT TypeOK
INVARIANT CausalOrdering
CHECK_DEADLOCK FALSE
