\* Buggy model: ghost dispatch + cross-run contamination.
\* Expected: CausalOrdering VIOLATED.
\* If TLC reports no error, the invariant is too weak.
SPECIFICATION SpecBuggy
CONSTANTS
  A = {a1, a2}
  R = {r1, r2}
CONSTRAINT LogBound
INVARIANT TypeOK
INVARIANT CausalOrdering
CHECK_DEADLOCK FALSE
