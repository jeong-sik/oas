\* Clean model: late/out-of-span ToolResult blocks are never accepted.
SPECIFICATION Spec
CONSTANTS
    ToolIds = {t1, t2}
INVARIANT TypeOK
INVARIANT NoLateToolResultsAccepted
INVARIANT PendingCoveredBeforeClose
CHECK_DEADLOCK FALSE
